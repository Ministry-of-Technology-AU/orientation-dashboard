// Google Calendar API client — server-side only
export async function getCalendarEvents(
  calendarId: string,
  timeMin: Date,
  timeMax: Date
) {
  // TODO: implement with googleapis service account
  void calendarId;
  void timeMin;
  void timeMax;
  return [];
}
