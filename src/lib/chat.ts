// Internal chatbot HTTP endpoint wrapper
export async function sendChatMessage(prompt: string, sessionId: string) {
  // TODO: replace with actual internal endpoint when API signature is provided
  void prompt;
  void sessionId;
  throw new Error("Chat endpoint not yet configured");
}
